name: Release (Linux/Windows)

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'

jobs:
  publish_release:
    name: "Publish Release"
    runs-on: [ ubuntu-latest ]
    steps:
      - uses: actions/checkout@v3

      - name: Get the latest tag
        run: echo "::set-output name=tag::$(git describe --tags --abbrev=0)"

      - name: Download the artifact
        uses: dawidd6/action-download-artifact@v2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          workflow: build.yml
          workflow_conclusion: success
          skip_unpack: true
          if_no_artifact_found: fail

      # - name: Create the release
      #   run: |
      #     gh release create ${{ steps.get-the-latest-tag.outputs.tag }}
      #     gh release upload --name PyActionSandbox_Linux --file dist/PyActionSandbox_Linux

      # - name: Call Other Workflow
      #   uses: yanis42/py_action_sandbox@v1
      # - name: Get Artifacts
      #   uses: actions/download-artifact@v3
      #   with:
      #     name: PyActionSandbox_Windows
      # - name: Create Release
      #   id: create_release
      #   uses: actions/create-release@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     tag_name: ${{ github.ref }}
      #     release_name: Release ${{ github.ref }}
      #     draft: false
      #     prerelease: false
      # - name: Upload Assets
      #   uses: actions/upload-release-asset@v1
      #   env:
      #     GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      #   with:
      #     upload_url: ${{ steps.create_release.outputs.upload_url }}
      #     asset_path: ./PyActionSandbox_Windows
      #     asset_name: PyActionSandbox_Windows
      #     asset_content_type: application/zip
          